DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DB?schema=public"
NEXT_PUBLIC_BASE_URL="http://localhost:3000"
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY="pk_test_xxx"
CLERK_SECRET_KEY="sk_test_xxx"
EMAIL_SERVER="smtp://user:pass@smtp.gmail.com:587" # optional if using BREVO_API_KEY
EMAIL_FROM="Mera Kommunikation <no-reply@merakommunikation.se>"
BREVO_API_KEY="your_brevo_smtp_api_key" # if set, uses smtp-relay.brevo.com

# 32-byte key for AES-256-GCM, base64 or hex. Example (base64):
# CREDENTIALS_ENCRYPTION_KEY=AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA=
CREDENTIALS_ENCRYPTION_KEY=qCRThGWSBY/cXuSEfiKrwg==

# Supabase Storage (create a bucket named "project-media", set it to public)
SUPABASE_URL="https://YOUR-PROJECT.supabase.co"
SUPABASE_PUBLIC_URL="https://YOUR-PROJECT.supabase.co" # optional, defaults to SUPABASE_URL
SUPABASE_BUCKET="project-management" # name of your storage bucket (must exist)
SUPABASE_MEDIA_SIGNED=false # set true if bucket is private; API will return signed URLs
SUPABASE_SIGNED_URL_TTL=3600 # seconds for signed URL validity
SUPABASE_ANON_KEY="YOUR_ANON_KEY" # not used server-side, useful for future client uploads
SUPABASE_SERVICE_ROLE_KEY="YOUR_SERVICE_ROLE_KEY" # used by server routes to upload/list/delete
